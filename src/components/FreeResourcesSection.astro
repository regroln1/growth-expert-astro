---
const resources = [
  {
    title: "Checklist",
    subtitle: "Alignez votre offre en 7 jours",
    description: "Guide étape par étape pour clarifier votre proposition de valeur et vous démarquer"
  },
  {
    title: "Template",
    subtitle: "Votre plan de croissance sur 90 jours", 
    description: "Modèle complet pour structurer votre développement avec timeline précise"
  },
  {
    title: "Audit Express",
    subtitle: "Diagnostiquez votre moteur d'acquisition",
    description: "Évaluation rapide de vos performances actuelles et axes d'amélioration"
  }
];
---

<section id="resources" class="py-20 lg:py-32 relative overflow-hidden">
  <!-- Simple dark background -->
  <div class="absolute inset-0 bg-navy-black"></div>
  
  <!-- Enhanced floating elements for depth -->
  <div class="absolute top-20 left-10 w-64 h-64 bg-blue-500/8 rounded-full blur-3xl float-slow"></div>
  <div class="absolute bottom-20 right-10 w-80 h-80 bg-indigo-500/10 rounded-full blur-3xl float-medium"></div>
  <div class="absolute top-1/2 left-1/4 w-48 h-48 bg-purple-500/6 rounded-full blur-2xl float-fast"></div>
  <div class="absolute top-1/3 right-1/3 w-56 h-56 bg-teal-500/6 rounded-full blur-3xl float-slow" style="animation-delay: 1s;"></div>

  <div class="relative z-10 container mx-auto px-6">
    <div class="max-w-6xl mx-auto text-center">
      <!-- Enhanced section header -->
      <div class="inline-flex items-center px-6 py-3 glass rounded-full mb-8 animate-fade-in">
        <svg class="w-5 h-5 mr-3 text-amber-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
        </svg>
        <span class="text-sm font-medium text-amber-400">Ressources Exclusives</span>
      </div>

      <h2 class="text-4xl lg:text-6xl font-bold text-white mb-8 font-crimson animate-fade-in" style="animation-delay: 0.1s;">
        Nos ressources gratuites pour
        <span class="text-gradient-gold block">dirigeants ambitieux</span>
      </h2>

      <p class="text-xl text-gray-300 font-crimson max-w-4xl mx-auto mb-16 animate-fade-in" style="animation-delay: 0.2s;">
        Commencez dès maintenant à transformer votre entreprise avec nos outils pratiques et éprouvés
      </p>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
        {resources.map((resource, index) => (
          <div
            class={`glass-strong p-10 rounded-3xl hover-lift transition-all duration-500 ${
              index === 0 ? 'animate-slide-left' : 
              index === 1 ? 'animate-fade-in' : 'animate-slide-right'
            }`}
            style={`animation-delay: ${0.3 + index * 0.2}s`}
          >
            <div class={`w-24 h-24 rounded-3xl flex items-center justify-center mx-auto mb-8 ${
              index === 0 ? 'bg-gradient-to-r from-gold-500 to-gold-600' : 
              index === 1 ? 'bg-gradient-to-r from-blue-500 to-blue-600' : 'bg-gradient-to-r from-purple-500 to-purple-600'
            }`}>
              {index === 0 && (
                <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 712-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                </svg>
              )}
              {index === 1 && (
                <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
              )}
              {index === 2 && (
                <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                </svg>
              )}
            </div>
            
            <h3 class="text-2xl font-bold text-white mb-3">
              {resource.title}
            </h3>
            
            <h4 class="text-xl font-semibold text-gradient-gold mb-6 font-crimson leading-tight">
              {resource.subtitle}
            </h4>
            
            <p class="text-gray-300 font-crimson mb-8 leading-relaxed text-lg">
              {resource.description}
            </p>

            <button class="w-full py-4 px-6 cta-secondary-dark text-white font-semibold rounded-full hover-lift transition-all duration-300">
              Télécharger gratuitement
            </button>
          </div>
        ))}
      </div>

      <!-- Enhanced main CTA -->
      <div class="animate-fade-in" style="animation-delay: 0.9s;">
        <div class="glass-strong p-8 rounded-3xl max-w-4xl mx-auto mb-8 hover-lift transition-all duration-500">
          <h3 class="text-2xl font-bold text-white mb-4 font-crimson">
            Accès immédiat et gratuit
          </h3>
          <p class="text-lg text-gray-300 font-crimson mb-6">
            Pas de spam, pas de vente forcée. Juste des outils pratiques pour faire progresser votre entreprise dès aujourd'hui.
          </p>
        </div>
        
        <a
          href="/ressources"
          class="inline-flex items-center px-12 py-6 cta-primary text-white font-semibold rounded-full hover-lift transition-all duration-300 text-xl"
        >
          Accéder aux ressources gratuites
          <svg class="ml-4 w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 712-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>