---
import SuiteDashForm from './SuiteDashForm.astro';

const contactInfo = [
  {
    title: "Téléphone",
    content: "+33 1 23 45 67 89",
    link: "tel:+33123456789"
  },
  {
    title: "Email",
    content: "contact@growthexpert.fr",
    link: "mailto:contact@growthexpert.fr"
  },
  {
    title: "Localisation",
    content: "Paris, France",
    link: null
  },
  {
    title: "Disponibilité",
    content: "Lun - Ven, 9h - 18h",
    link: null
  }
];

const benefits = [
  "Consultation gratuite de 30 minutes",
  "Approche personnalisée et sur-mesure",
  "Résultats mesurables et garantis",
  "Accompagnement continu et flexible"
];
---

<section id="contact" class="py-20 lg:py-32 relative overflow-hidden">
  <!-- Simple dark background -->
  <div class="absolute inset-0 bg-navy-black"></div>
  
  <!-- Enhanced floating elements for depth -->
  <div class="absolute top-20 left-10 w-64 h-64 bg-blue-500/30 rounded-full blur-3xl float-slow"></div>
  <div class="absolute bottom-20 right-10 w-80 h-80 bg-purple-500/30 rounded-full blur-3xl float-medium"></div>
  <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-48 h-48 bg-teal-500/25 rounded-full blur-2xl float-fast"></div>

  <div class="relative z-10 container mx-auto px-6">
    <!-- Enhanced section header -->
    <div class="text-center mb-16">
      <div class="inline-flex items-center px-6 py-3 glass rounded-full mb-8 animate-fade-in">
        <svg class="w-4 h-4 mr-2 text-amber-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
        </svg>
        <span class="text-sm font-medium text-amber-400">Contact</span>
      </div>
      
      <h2 class="text-4xl lg:text-6xl font-bold text-white mb-6 font-crimson animate-fade-in" style="animation-delay: 0.1s;">
        Transformons ensemble
        <span class="text-gradient-gold block">votre vision en réalité</span>
      </h2>
      
      <p class="text-xl text-gray-300 max-w-3xl mx-auto font-crimson animate-fade-in" style="animation-delay: 0.2s;">
        Prenez rendez-vous pour une consultation gratuite et découvrons comment 
        accélérer la croissance de votre entreprise.
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16">
      <!-- Enhanced contact form -->
      <div class="glass-strong p-8 lg:p-12 rounded-3xl hover-lift transition-all duration-500 animate-slide-left" style="animation-delay: 0.3s;">
        <h3 class="text-2xl font-bold text-white mb-6">
          Démarrons la conversation
        </h3>

        <SuiteDashForm
          formUrl="https://your-suitedash-url.com/frm/contact"
          height="600px"
          trustText="🔒 Vos informations sont sécurisées et ne seront jamais partagées."
          loadingText="Chargement du formulaire de contact..."
          containerClass="bg-transparent"
        />
      </div>

      <!-- Enhanced contact info -->
      <div class="space-y-8 animate-slide-right" style="animation-delay: 0.5s;">
        <div class="glass-strong p-8 rounded-3xl hover-lift transition-all duration-500">
          <h3 class="text-2xl font-bold text-white mb-6">
            Informations de contact
          </h3>

          <div class="space-y-6">
            {contactInfo.map((info, index) => (
              <div class="flex items-start">
                <div class={`w-12 h-12 rounded-xl flex items-center justify-center mr-4 flex-shrink-0 ${
                  index === 0 ? 'bg-gradient-to-r from-gold-500 to-gold-600' : 
                  index === 1 ? 'bg-gradient-to-r from-blue-500 to-blue-600' : 
                  index === 2 ? 'bg-gradient-to-r from-purple-500 to-purple-600' : 'bg-gradient-to-r from-teal-500 to-teal-600'
                }`}>
                  {index === 0 && (
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                    </svg>
                  )}
                  {index === 1 && (
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                    </svg>
                  )}
                  {index === 2 && (
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    </svg>
                  )}
                  {index === 3 && (
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                  )}
                </div>
                <div>
                  <h4 class="font-semibold text-white mb-1">{info.title}</h4>
                  {info.link ? (
                    <a
                      href={info.link}
                      class="text-gray-300 hover:text-gold-400 transition-colors"
                    >
                      {info.content}
                    </a>
                  ) : (
                    <p class="text-gray-300">{info.content}</p>
                  )}
                </div>
              </div>
            ))}
          </div>
        </div>

        <!-- Enhanced benefits -->
        <div class="glass-strong p-8 rounded-3xl hover-lift transition-all duration-500">
          <h3 class="text-2xl font-bold text-white mb-6">
            Pourquoi me choisir ?
          </h3>

          <div class="space-y-4">
            {benefits.map((benefit) => (
              <div class="flex items-center">
                <svg class="w-5 h-5 text-green-400 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <span class="text-gray-300">{benefit}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('contact-form') as HTMLFormElement;
    if (form) {
      form.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // Simple form handling
        const formData = new FormData(this);
        const data = Object.fromEntries(formData);
        
        // Simulate sending
        const button = this.querySelector('button[type="submit"]') as HTMLButtonElement;
        if (button) {
          const originalText = button.innerHTML;
          
          button.innerHTML = `
            <div class="w-5 h-5 border-2 border-white/30 border-t-white rounded-full animate-spin mr-2"></div>
            Envoi en cours...
          `;
          button.disabled = true;
          
          setTimeout(() => {
            alert('Message envoyé ! Je vous recontacterai dans les 24h. Merci pour votre intérêt !');
            form.reset();
            button.innerHTML = originalText;
            button.disabled = false;
          }, 1000);
        }
      });
    }
  });
</script>